# Copy to .env.local and fill in your real keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# LaTeX engine: use tectonic (recommended, small) or pdflatex if MacTeX is installed
LATEX_ENGINE=tectonic

# Backend port (default)
PORT=3001
